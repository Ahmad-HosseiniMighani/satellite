# satellite -- Gemini/Antigravity Instructions

Read `AGENTS.md` in this directory and follow it exactly. That file is the
canonical source — this one exists only so Gemini-based CLIs (and any other
CLI that looks for a CLI-specific entrypoint instead of `AGENTS.md` directly)
find something on session start.
